defaults:
  - train_libero_long
  - _self_

folder_tag: 1
eval_tag: test
after: 0
before: -1
use_ema: false

# eval-only overrides
n_videos: 10
n_eval_episodes: 50
n_eval_processes: 5
seed: 1111111
restore_idm_snapshot_path: null
batch_size: 16

eval_type: default

name: eval_libero_long

hydra:
  run:
    dir: ./experiments/${experiment_folder}_eval${folder_tag}/${experiment_name}_${eval_tag}
